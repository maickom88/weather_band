name: weather_band
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  either_dart: ^0.1.2
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.11

flutter:
  uses-material-design: true
  assets:
    - assets/images/